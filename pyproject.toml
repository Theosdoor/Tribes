[project]
name = "tribes"
version = "0.1.0"
description = "Tribes game web interface"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pytest",
    "pytest-asyncio",
]
